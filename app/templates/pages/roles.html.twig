{% extends "pages/abstract/dashboard.html.twig" %}

{% block stylesheets_page %}
    <!-- Page-specific CSS asset bundle -->
    {{ encore_entry_link_tags('page.roles') }}
{% endblock %}

{# Overrides blocks in head of base template #}
{% block page_title %}{{ translate("ROLE", 2)}}{% endblock %}

{% block page_description %}{{ translate("ROLE.PAGE_DESCRIPTION")}}{% endblock %}

{% block body_matter %}
    <div class="row">
        <div class="col-md-12">
            <div id="widget-roles" class="box box-primary">
                <div class="box-header">
                    <h3 class="box-title"><i class="fa-solid fa-fw fa-id-card"></i> {{translate('ROLE', 2)}}</h3>
                    {% include "tables/table-tool-menu.html.twig" %}
                </div>
                <div class="panel-body">
                    {% include "tables/roles.html.twig" with {
                            "table" : {
                                "id" : "table-roles"
                            }
                        }
                    %}
                </div>
                <div class="box-footer">
                    <button type="button" class="btn btn-success js-role-create">
                        <i class="fa-solid fa-plus-square"></i>  {{translate("ROLE.CREATE")}}
                    </button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block scripts_page %}
    <!-- Include validation rules -->
    <script>
    {% include "pages/partials/page.js.twig" %}
    </script>

    <!-- Include page-specific JS -->
    {{ encore_entry_script_tags('page.roles') }}

{% endblock %}
