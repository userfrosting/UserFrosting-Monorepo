import type { PermissionInterface } from '@userfrosting/sprinkle-account/interfaces'

/**
 * Create PermissionApi interface, based on PermissionInterface
 */
export interface PermissionApi extends PermissionInterface {}
